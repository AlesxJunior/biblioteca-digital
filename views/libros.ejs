<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catálogo - Biblioteca Digital</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">📚 Biblioteca Digital</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/">Inicio</a>
                <a class="nav-link active" href="/libros">Catálogo</a>
                <a class="nav-link" href="/agregar">Agregar Libro</a>
		<a class="nav-link" href="/dashboard">Dashboard</a>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <h2 class="mb-4">📖 Catálogo de Libros</h2>
        
        <div class="row">
            <% libros.forEach(libro => { %>
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0"><%= libro.titulo %></h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Autor:</strong> <%= libro.autor %></p>
                        <p><strong>Año:</strong> <%= libro.anio %></p>
                        <p><strong>Género:</strong> <%= libro.genero %></p>
                    </div>
                    <div class="card-footer">
                        <a href="/libro/<%= libro.id %>" class="btn btn-outline-primary btn-sm">Ver detalles</a>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>

        <div class="text-center mt-4">
            <a href="/agregar" class="btn btn-success">➕ Agregar Nuevo Libro</a>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-3 mt-5">
        <p class="mb-0">Biblioteca Digital © 2025 - Proyecto Cloud PaaS</p>
    </footer>
</body>
</html>
